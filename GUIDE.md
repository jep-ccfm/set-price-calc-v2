# 세트공급가 계산기 사용 가이드

**바로가기 →** https://jep-ccfm.github.io/set-price-calc-v2/

설치할 것 없이 링크만 열면 됩니다. PC·모바일 모두 사용 가능합니다.

---

## 1. 이 도구는 왜 쓰나요?

거래처에 세트 상품을 제안할 때, **목표 마진율을 지키는 공급가를 자동으로 계산**해주는 도구입니다.

기존에는 엑셀로 매번 계산했고, 어떤 차수에 어떤 세트를 제안했는지는 각자 파일로 흩어져 있었습니다. 이 도구는 두 가지를 한 번에 해결합니다.

| 목적 | 무엇을 하나 |
|---|---|
| **① 공급가 계산** | 품목·수량만 넣으면 목표 마진율을 충족하는 세트 공급가 자동 산출 |
| **② 히스토리 관리** | 업체별·차수별로 제안 구성과 판매 실적을 쌓아두고 팀 전체가 공유 |

### 꼭 알아둘 점: 무엇이 공유되나요?

- **팀 공용(모두에게 보임)** — 세트 저장소의 차수·제안 구성·판매 집계·메모, 그리고 품목별 생산원가(DB)
- **개인용(나만 보임)** — 계산기 화면에 지금 입력 중인 값

즉 저장소에 정리한 내용은 **다른 팀원이 링크만 열어도 그대로 보입니다.**

---

## 2. 화면 구조 한눈에 보기

왼쪽 검은색 네비게이션 바로 이동합니다.

- **🧮 계산기** — 공급가를 계산하는 곳
- **📚 세트 저장소 (라이브러리)** — 히스토리를 쌓는 곳
  - 🇯🇵 **일본라방** — 안에서 **메라블 / 루솔브** 브랜드를 나눠서 관리
  - 🇭🇰 **홍콩 라이브 커머스**
  - 📦 **레디영**

> **메라블** = 루비알엔 · 유쎄라블 · 포어시그널 · 비타터닝 라인
> **루솔브** = 비오티아 라인
> 두 브랜드는 차수·구성·판매집계가 **완전히 따로** 관리됩니다.

네비게이션 옆 숫자 배지는 그 업체에 저장된 **차수 개수**입니다.

---

## 3. 계산기 사용법 (기본 흐름)

### STEP 1. 목표 마진율 입력

기본값은 **0.68 (68%)** 입니다. 이 숫자 하나만 바꾸면 아래 전체가 자동으로 다시 계산됩니다.

> 마진율 = (공급가 − 원가) ÷ 공급가

### STEP 2. 세트 구성 입력

1. **1EA 공급가 올림 단위**를 고릅니다 — `100원` 또는 `1,000원`
2. 표에서 **품목을 선택**하고 **수량**을 입력합니다
3. 품목이 더 필요하면 **`+ 품목 추가`**

입력하는 즉시 품목별 원가소계 · 1EA 공급가 · 공급가소계 · 마진율이 채워집니다.

**계산 방식 (중요)**
```
1EA 공급가 = 올림( 원가 ÷ (1 − 목표마진율), 올림단위 )
세트 공급가 = 품목별 공급가소계의 합
```
세트 전체를 한 번에 나누지 않고 **품목마다 각각 올림 후 합산**합니다. 그래야 개별 품목도 항상 목표 마진율 이상이 보장됩니다.

### STEP 3. 계산 결과 확인

- **★ 세트 공급가** — 실제로 제안할 금액
- **실제 마진율** — 올림 때문에 목표보다 조금 높게 나옵니다 (정상)
- 세트 총 생산원가 / 이론상 최소 공급가 / 마진액도 함께 표시

### STEP 4. 견적용 라운드업

`100원 단위` 또는 `1,000원 단위`를 선택하면 세트 합계를 딱 떨어지게 올림한 금액이 나옵니다. 실무 견적서에 쓰기 좋은 숫자입니다.

### STEP 5. 결과 활용

- **`결과 요약 복사`** — 구성·마진·공급가가 정리된 텍스트가 복사됩니다. 메일·메신저에 그대로 붙여넣기
- **`세트 구성 초기화`** — 표를 비우고 새로 시작
- **`📚 차수 저장`** — 이 구성을 저장소에 기록 (아래 4번 참고)

---

## 4. 저장소에 기록하기 (차수 저장)

계산이 끝났으면 **`📚 차수 저장 (라이브러리에 담기)`** 를 누릅니다.

| 입력 항목 | 설명 |
|---|---|
| **업체** | 일본라방 / 홍콩 라이브 커머스 / 레디영 |
| **브랜드** | 일본라방을 고르면 나타남 — 메라블 / 루솔브 |
| **차수** | 기존 차수 선택, 또는 `＋ 새 차수 생성` |
| **구성 이름** | 예: `3종 기획세트`, `봄 프로모션 A안` |
| **제안처** | **내부 제안(우리)** / **업체 제안** 중 선택 |

저장하면 해당 업체 화면으로 이동하며 카드로 쌓입니다.

> **제안처 구분이 왜 중요한가요?**
> 우리가 먼저 제안한 건인지, 업체 쪽에서 요청해온 건인지 나중에 헷갈리기 쉽습니다. 카드에 배지로 표시되니 꼭 골라주세요.

---

## 5. 저장소 화면 읽는 법 (히스토리 파악)

업체를 클릭하면 위에서부터 이렇게 구성돼 있습니다.

### ① 브랜드 토글 (일본라방만)
메라블 / 루솔브 중 볼 브랜드를 클릭합니다. 각 탭에 차수 개수가 표시됩니다.

### ② 차수 바
`1차` `2차` `3차` … 알약 버튼입니다. 클릭하면 그 차수 내용이 아래에 펼쳐집니다.

- **`＋ 다음차로 넘어가기`** — 새 차수를 만듭니다 (번호 자동 부여)
- **`차수 이름수정`** — 예: `8차 (7/28 발주)` 처럼 메모를 붙여도 됩니다.
  이름 속 **숫자를 인식해 자동으로 순서 정렬**되니 순서 걱정 안 하셔도 됩니다.
- **`차수 삭제`** — 그 차수의 구성·판매 실적이 모두 삭제됩니다 (주의)

### ③ 📊 판매 집계 (접이식 토글)
차수 바로 아래 띠를 클릭하면 펼쳐집니다. 접힌 상태에서도 **총 세트 수 · 총 매출액**이 요약으로 보입니다.

### ④ 제안 세트 구성
그 차수에 저장된 세트 카드들입니다. 카드에는 이렇게 표시됩니다.

- 구성 이름과 **내부 제안 / 업체 제안** 배지
- **세트 공급가** (수기 입력했다면 `수기` 배지)
- 품목 종수 · 목표마진(또는 실제마진) · 총원가
- 세트에 들어간 품목과 수량 목록

---

## 6. 판매 실적 입력하기 (발주일별)

판매 집계 토글을 열면, 세트마다 블록이 하나씩 있습니다.

### 기본 입력
각 세트 블록에서 **발주일**과 **판매 세트 수**를 입력합니다.

### 같은 세트가 추가 발주로 또 들어왔다면?
**`＋ 발주일 추가`** 를 누르고 날짜와 수량을 한 줄 더 넣으면 됩니다.

> ⚠️ **세트를 복제하지 마세요.** 예전에는 날짜별로 똑같은 세트를 여러 개 만들어야 했지만, 이제는 **한 세트에 발주일만 계속 추가**하면 됩니다.

**예시 — 루비알엔 피코샷 세트 (26,400원)**
| 발주일 | 판매 세트 수 |
|---|---|
| 2026-03-24 | 544 |
| 2026-03-26 | 93 |
| 2026-03-30 | 44 |

→ 블록 우측에 **681세트 · 17,978,400원** 으로 자동 합산

### 자동으로 집계되는 것
- **세트별 매출액** = 판매 세트 수 합계 × 세트 공급단가
- **총 나간 품목별 수량** — 세트 구성을 기준으로 자동 환산됩니다
  (피코샷이 세트당 2개인데 681세트 팔렸다면 → 1,362개)
- 맨 아래 **총 판매 세트 수 · 총 매출액**

---

## 7. 저장소에서 직접 추가·수정하기

계산기를 거치지 않고 저장소에서 바로 작업할 수도 있습니다. **업체가 제안해온 구성**을 기록할 때 특히 편합니다.

### 구성 추가
`제안 세트 구성` 우측의 **`＋ 구성 추가`** 클릭

| 항목 | 설명 |
|---|---|
| 구성 이름 | 예: `3종 기획세트 (업체 요청안)` |
| 제안처 | 내부 제안 / 업체 제안 |
| 목표 마진율 / 올림 단위 | 기본 0.68 · 100원 |
| **세트 공급가 수기입력** | 아래 설명 참고 (비우면 자동 계산) |
| 품목 구성 | 품목명 + 수량. **원가DB에 없는 품목(사은품·파우치 등)도 자유롭게 입력 가능** |

### 💡 세트 공급가 수기입력은 언제 쓰나요?

지난 차수처럼 **포괄 마진으로 계산해서 자동 계산값과 안 맞는 경우**에 씁니다.

- 금액을 입력하면 → 그 금액이 세트 공급가가 되고, **실제 마진율도 그 금액 기준**으로 다시 표시됩니다
- 비워두면 → 지금처럼 품목별 자동 합산

### 카드에서 할 수 있는 것
- **`계산기로 불러오기`** — 그 구성을 계산기에 그대로 올려 마진·수량을 다시 조정
- **`수정`** — 이름·품목·수량·공급가 변경
- **`복제`** — 비슷한 안을 하나 더 만들 때 (※ 날짜 구분용으로는 쓰지 마세요 → 6번 참고)
- **`삭제`**

---

## 8. 📝 차수 메모

화면 **오른쪽에 붙어 있는 `📝 메모` 탭**을 클릭하면 열립니다. 스크롤을 내려도 계속 따라다닙니다.

- **차수마다 별도**로 저장됩니다
- 발주 특이사항, 업체 요청 내용, 정산 참고사항 등을 적어두세요
- 내용이 있으면 탭에 **노란 점**이 표시됩니다
- 거슬리면 **✕** 로 접어두면 됩니다 (선택 상태 유지)
- **팀 공유**됩니다

---

## 9. 품목별 생산원가 관리 (DB)

계산기 화면 맨 아래 **`DB 품목별 생산원가 관리`** 를 클릭하면 펼쳐집니다.

- 품목명·원가 수정, 새 품목 추가, 삭제 가능
- 수정하면 **모든 계산에 즉시 반영**됩니다

> ⚠️ **원가DB는 팀 공용입니다.** 여기서 값을 바꾸면 **모든 팀원의 계산 결과가 함께 바뀝니다.** 확실한 기준값일 때만 수정해주세요.

---

## 10. 팀 공유와 동기화

왼쪽 아래에서 동기화 상태를 볼 수 있습니다.

| 표시 | 의미 |
|---|---|
| ✓ 최신 동기화됨 | 공용 데이터를 정상적으로 불러옴 |
| ⟳ 저장 중… → ✓ 저장됨 · 팀 공유완료 | 내 변경사항이 팀에 반영됨 |
| ⚠ 공유 서버 연결 실패 | 네트워크 문제. 로컬에만 저장되니 나중에 다시 접속해주세요 |

- 저장은 **자동**입니다. 따로 저장 버튼을 누를 필요 없습니다.
- 다른 사람이 방금 올린 내용을 바로 보려면 **`↻ 최신 불러오기`** 를 누르세요.
- 다른 탭 갔다가 돌아오면 자동으로 다시 불러옵니다.

---

## 11. 자주 묻는 질문

**Q. 실제 마진율이 목표보다 높게 나와요.**
정상입니다. 품목마다 올림(100원/1,000원)을 하기 때문에 목표 마진율보다 조금 높아집니다.

**Q. 품목에 `(원가없음)` 이라고 떠요.**
원가DB에 없는 품목입니다. 기록은 되지만 **공급가 자동 계산에서는 제외**됩니다. 사은품처럼 원가를 안 잡는 항목이면 그대로 두어도 되고, 계산에 넣으려면 DB에 품목을 추가하세요.

**Q. 내가 저장한 게 다른 사람에게 안 보여요.**
상대방에게 **`Ctrl` + `Shift` + `R`** (강력 새로고침)을 요청하거나, 왼쪽 아래 **`↻ 최신 불러오기`** 를 누르게 해주세요.

**Q. 화면이 예전 버전 같아요.**
브라우저 캐시입니다. **`Ctrl` + `Shift` + `R`** 로 새로고침하면 최신 화면이 나옵니다.

**Q. 계산기에 입력하던 값이 다른 PC에서는 안 보여요.**
계산기 입력값은 **개인용(내 브라우저 저장)** 입니다. 공유하려면 `📚 차수 저장`으로 저장소에 담으세요.

**Q. 차수 이름을 바꿨더니 순서가 어떻게 되나요?**
이름 속 숫자를 인식해 **자동으로 정렬**됩니다. `8차 (7/28 발주)` 처럼 뒤에 설명을 붙여도 8번 자리에 잘 들어갑니다. 숫자가 없는 이름(예: `특별방송`)은 맨 뒤로 갑니다.

---

## 12. 추천 사용 흐름 요약

```
새로운 제안을 준비할 때
  계산기에서 마진율·품목·수량 입력
      ↓
  세트 공급가 확인 → 라운드업 금액으로 견적
      ↓
  📚 차수 저장 → 업체·브랜드·차수 선택
      ↓
  거래처에 제안 (결과 요약 복사 활용)

발주가 들어왔을 때
  저장소 → 해당 업체·브랜드·차수
      ↓
  📊 판매 집계 열기 → 세트별 발주일 + 판매 세트 수 입력
      ↓
  추가 발주가 오면 ＋ 발주일 추가 (세트 복제 X)
      ↓
  📝 메모에 특이사항 기록

다음 차수로 넘어갈 때
  ＋ 다음차로 넘어가기 → 새 차수에서 다시 시작
```


---

## 13. 레디영 · 개인약국 — 거래명세서 자동 집계

좌측 **📦 레디영** 또는 **💊 신사 타워약국 / 압구정 생생 온누리약국** 에서 사용합니다.

### 발주서(거래명세서) 올리기
1. **`📁 월별 폴더 올리기`** — 그 달 폴더를 통째로 선택하면 안에 있는 PDF를 모두 읽습니다
2. 또는 **`PDF 파일 선택`** / 창 안으로 **드래그&드롭**

올리면 PDF에서 **지점 · 품목 · 수량 · 단가 · 공급가액 · 합계**를 자동으로 읽어 **월별로 분류**합니다. (같은 파일을 다시 올리면 중복은 자동으로 건너뜁니다.)

### 확인할 수 있는 것
- **월 탭** — 26년 8월, 26년 7월 … 클릭해서 그 달로 전환
- **요약** — 매출 합계(VAT 포함) · 공급가액 · 총 수량 · 명세서 건수
- **지점별 판매** — 지점별 건수/수량/매출/비중
- **품목별 판매** — 품목별 총 수량과 공급가액
- **명세서 상세** — 개별 명세서 목록 (✕로 개별 삭제 가능)

> 수량 합계가 안 맞는 등 이상이 있으면 **⚠ 확인 필요**로 표시되니 그 건만 확인하시면 됩니다.

### 정산 시트 바로가기
레디영 화면 맨 위 **`📊 정산 시트 바로가기`** 버튼으로 구글 시트를 바로 열 수 있습니다.

---

## 14. 개인약국 관리

### 약국 정보
**`📋 약국정보 확인 / 수정`** 버튼으로 입력·수정합니다.
약국명 · 요양기호 · 사업자등록번호 · 대표자 · 전화번호 · FAX · 이메일 · 주소 · 비고

### 서류 첨부
**`＋ 파일 첨부`** 로 사업자등록증, 약국 명함, 계약서 등을 올립니다.
- 이미지·PDF 모두 가능 (1개당 최대 25MB)
- 이미지는 **썸네일**로 보이고, 클릭하면 **크게 미리보기** (PDF도 바로 열림)
- `열기`로 새 창에서 보기, `삭제`로 제거
- **팀 공유**됩니다

### 발주 · 거래 내역
13번과 동일하게 거래명세서 PDF를 올려 월별로 집계합니다.

---

### ⚠️ 데이터 취급 주의

이 도구는 **링크를 아는 사람은 누구나 열람·수정할 수 있습니다.** 공급가·판매 실적 등 내부 정보가 담기므로 **링크를 사외로 공유하지 마세요.**
